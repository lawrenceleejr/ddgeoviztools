# Core geometry library — reads GDML, builds VTK scene, drives OBJ/GLTF export
pyg4ometry>=1.3.5

# ANTLR4 runtime used by pyg4ometry's expression parser.
# Must be a 4.x release; pyg4ometry ships pre-generated ANTLR4 grammars and
# will raise an ImportError if this version is incompatible.
antlr4-python3-runtime>=4.11,<5

# XML parsing for the GDML splitter
lxml>=4.9

# VTK — pulled in as a pyg4ometry dep, pinned here for visibility.
# The wheel for Python 3.10 on Linux is ~100 MB and includes Mesa bindings.
vtk>=9.2

# Numerical / scientific stack
numpy>=1.24
scipy>=1.10
sympy>=1.7

# Blender Python API — Blender 4.0 is the last release that uses Python 3.10.
# bpy and vtk are never imported in the same process (lazy imports per subcommand)
# so there is no runtime conflict between the two OpenGL stacks.
#bpy>=4.0,<4.1

# Mesh I/O for the Blender scene builder (reads OBJ/GLTF/VTP → vertex/face arrays)
trimesh>=4.0
