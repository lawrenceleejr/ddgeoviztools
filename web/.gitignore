node_modules
dist
dist-ssr
*.local
.vite
playwright-report
test-results

# Generated by the Cycles bake (scripts/bake_web.sh / CI). Not committed —
# produced fresh by the deploy pipeline, or locally via `npm run bake`.
public/baked/
